import type { CertificationPack } from "./pack.js";

// Curso PREPARATÓRIO (não oficial) para a futura certificação
// "Claude Certified Developer", anunciada pela Anthropic mas ainda não lançada.
//
// IMPORTANTE — honestidade: este pack NÃO é o blueprint oficial do exame. Ele é
// construído a partir da documentação pública da Anthropic (docs.anthropic.com /
// platform.claude.com / code.claude.com) e dos guias de engenharia da Anthropic.
// O conteúdo pode não refletir o blueprint final quando a certificação for
// lançada. A primeira lição do módulo 1 deixa esse aviso explícito.
//
// Convenções de tags (kebab-case): "contexto" | "api" | "sdk" | "agentes" |
// "tool-use" | "agent-sdk" | "rag" | "embeddings" | "citations" | "avaliacao" |
// "erros" | "prompt-caching" | "observabilidade" | "seguranca".
//
// Precisão: ensinamos os tiers de modelo (Opus / Sonnet / Haiku) e mandamos à
// doc oficial em vez de fixar preços ou IDs de modelo que mudam com o tempo.

export const ccaDeveloperPrep: CertificationPack = {
  slug: "cca-developer-prep",
  title: "Claude Certified Developer — Preparatório (não oficial)",
  description:
    "Curso PREPARATÓRIO baseado na documentação oficial da Anthropic para a futura certificação de Developer (anunciada, ainda não lançada). Aprofunda construção de agentes, Agent SDK, avaliação, RAG e confiabilidade em produção. Não é o exame oficial.",
  version: 1,
  modules: [
    // =====================================================================
    // MÓDULO 1 — CONTEXTO DA CERTIFICAÇÃO E FUNDAMENTOS DO DESENVOLVEDOR
    // =====================================================================
    {
      title: "Contexto da certificação e fundamentos do desenvolvedor",
      lessons: [
        {
          title: "Aviso e contexto: o que é (e o que não é) este curso",
          readingMd: [
            "# Aviso e contexto: leia antes de tudo",
            "",
            "> **Este curso é PREPARATÓRIO e NÃO OFICIAL.** Ele não é o blueprint do exame, não foi revisado pela Anthropic e não garante aprovação em nenhuma prova.",
            "",
            "## O que a Anthropic anunciou",
            "",
            "Em **outubro de 2025**, a Anthropic anunciou o **Claude Partner Network** e, junto dele, uma trilha de **certificações** para três perfis profissionais:",
            "",
            "- **Developers** (desenvolvedores) — quem constrói aplicações e agentes com a Claude API e o Agent SDK;",
            "- **Architects** (arquitetos de solução) — quem desenha aplicações de produção com Claude;",
            "- **Sellers** (vendas/pré-vendas) — quem posiciona e vende soluções com Claude.",
            "",
            "## O que JÁ foi lançado vs. o que ainda NÃO foi",
            "",
            "No momento em que este material foi escrito, **apenas a certificação \"Claude Certified Architect – Foundations\" foi efetivamente lançada**. As certificações específicas para **developers** e **sellers**, e os níveis mais avançados de architect, foram **anunciados mas ainda não estavam disponíveis** — a comunicação oficial fala em \"novas certificações ao longo do ano\", sem nome final, blueprint público nem data confirmada para a prova de Developer.",
            "",
            "Ou seja: **a \"Claude Certified Developer\" ainda não existe como exame**. Este curso antecipa os temas que, com alta probabilidade, um desenvolvedor precisa dominar — porque eles vêm direto da documentação oficial de como se constrói com Claude.",
            "",
            "## De onde veio o conteúdo deste curso",
            "",
            "Todo o material foi destilado da **documentação pública da Anthropic** e dos **guias de engenharia** da empresa, incluindo:",
            "",
            "- a referência da **Claude API / Messages API**;",
            "- a documentação de **tool use** (uso de ferramentas);",
            "- a documentação do **Claude Agent SDK**;",
            "- o guia de **definição de critérios de sucesso e avaliações (evals)**;",
            "- a documentação de **prompt caching** e de **erros da API**;",
            "- o artigo de engenharia **\"Building effective agents\"**.",
            "",
            "Cada lição termina com um callout `> Leitura oficial:` apontando para a página real. **Sempre confirme na fonte oficial** — a doc muda, e o blueprint do exame, quando sair, tem prioridade sobre qualquer coisa aqui.",
            "",
            "## Honestidade sobre versões de modelo e preços",
            "",
            "Os **IDs de modelo** (ex.: o identificador exato da geração atual de Opus) e os **preços** mudam com frequência. Por isso este curso ensina os **tiers**:",
            "",
            "- **Opus** — o tier mais capaz, para raciocínio complexo e agentes difíceis;",
            "- **Sonnet** — equilíbrio entre capacidade, latência e custo (cavalo de batalha de produção);",
            "- **Haiku** — o tier mais rápido e barato, para tarefas de alto volume e baixa complexidade.",
            "",
            "Para o **ID exato** do modelo e o **preço atual**, consulte sempre a tabela oficial de modelos. Não confie em números decorados.",
            "",
            "## Link oficial do anúncio",
            "",
            "Leia o anúncio do programa de parceiros e certificações aqui: https://www.anthropic.com/news/claude-partner-network",
            "",
            "> Leitura oficial: https://www.anthropic.com/news/claude-partner-network",
          ].join("\n"),
          flashcards: [
            {
              front: "A certificação \"Claude Certified Developer\" já existe como exame?",
              back: "Não. Foi ANUNCIADA junto do Claude Partner Network, mas até a escrita deste material apenas a \"Claude Certified Architect – Foundations\" havia sido lançada. Este curso é preparatório, não o blueprint oficial.",
              tags: ["contexto"],
            },
            {
              front: "Quais três perfis de certificação a Anthropic anunciou?",
              back: "Developers (desenvolvedores), Architects (arquitetos de solução) e Sellers (vendas).",
              tags: ["contexto"],
            },
            {
              front: "Por que este curso ensina tiers (Opus/Sonnet/Haiku) em vez de IDs e preços fixos?",
              back: "Porque IDs de modelo e preços mudam com frequência. Os tiers são estáveis conceitualmente; para o ID e o preço exatos, sempre consulte a tabela oficial de modelos.",
              tags: ["contexto"],
            },
            {
              front: "Qual é a fonte do conteúdo deste curso preparatório?",
              back: "A documentação pública da Anthropic (Claude API, tool use, Agent SDK, evals, prompt caching, erros) e os guias de engenharia, como \"Building effective agents\". Não é material oficial do exame.",
              tags: ["contexto"],
            },
          ],
          questions: [
            {
              prompt:
                "Qual afirmação é HONESTA sobre o status da certificação de Developer no momento desta escrita?",
              options: [
                "É o blueprint oficial do exame, revisado pela Anthropic",
                "Foi anunciada mas ainda não lançada; só a Architect – Foundations estava disponível",
                "Já foi lançada e substitui a Architect – Foundations",
                "Nunca foi mencionada pela Anthropic",
              ],
              correctIndex: 1,
              explanation:
                "A Anthropic anunciou certificações para developers/architects/sellers, mas até a escrita deste material apenas a Architect – Foundations havia sido lançada. Este curso é preparatório, não oficial.",
              difficulty: 1,
              tags: ["contexto"],
            },
            {
              prompt: "Por que o curso não fixa preços nem IDs exatos de modelo?",
              options: [
                "Porque a Anthropic proíbe citar tiers",
                "Porque preços e IDs mudam com frequência; o curso ensina tiers e remete à tabela oficial",
                "Porque os modelos não têm preço",
                "Porque Opus, Sonnet e Haiku custam o mesmo",
              ],
              correctIndex: 1,
              explanation:
                "IDs e preços mudam. O conceito de tier (Opus mais capaz, Sonnet equilibrado, Haiku mais rápido/barato) é estável; o valor exato deve vir da doc oficial.",
              difficulty: 1,
              tags: ["contexto"],
            },
            {
              prompt:
                "Onde encontrar a informação autoritativa sobre o blueprint da certificação de Developer quando ela for lançada?",
              options: [
                "Neste curso preparatório",
                "Em fóruns não oficiais",
                "Na comunicação oficial da Anthropic / Claude Partner Network",
                "No histórico de commits deste repositório",
              ],
              correctIndex: 2,
              explanation:
                "O blueprint oficial, quando existir, virá da Anthropic. Este curso explicitamente se rotula como não oficial e manda confirmar na fonte.",
              difficulty: 1,
              tags: ["contexto"],
            },
          ],
        },
        {
          title: "Messages API: o núcleo de toda aplicação com Claude",
          readingMd: [
            "# Messages API: o núcleo de toda aplicação com Claude",
            "",
            "Um desenvolvedor Claude vive em torno da **Messages API** (`POST /v1/messages`). Tudo — chat, RAG, agentes, tool use — é construído sobre ela.",
            "",
            "## Anatomia de uma requisição",
            "",
            "- **`model`** — o tier/modelo (família Opus, Sonnet ou Haiku). Use o **ID exato** da doc oficial; aqui raciocinamos por tier.",
            "- **`max_tokens`** — limite de tokens **gerados** na resposta (obrigatório). Não limita a entrada. Se atingido, `stop_reason` vem como `\"max_tokens\"`.",
            "- **`messages`** — array de turnos alternados; cada item tem `role` (`\"user\"` ou `\"assistant\"`) e `content`. A conversa começa por `user`.",
            "- **`system`** — campo de topo (fora de `messages`) com o **system prompt**: papel, regras, formato. Não é um turno de usuário.",
            "",
            "## Exemplo (TypeScript — `@anthropic-ai/sdk`)",
            "",
            "```ts",
            "import Anthropic from \"@anthropic-ai/sdk\";",
            "",
            "const client = new Anthropic(); // lê ANTHROPIC_API_KEY do ambiente",
            "",
            "const message = await client.messages.create({",
            "  model: \"<id-do-modelo-da-doc-oficial>\", // ex.: um modelo do tier Sonnet",
            "  max_tokens: 1024,",
            "  system: \"Você é um tutor de certificações Anthropic. Responda em pt-BR.\",",
            "  messages: [{ role: \"user\", content: \"Explique o parâmetro max_tokens.\" }],",
            "});",
            "",
            "console.log(message.content); // lista de content blocks",
            "console.log(message.usage);   // { input_tokens, output_tokens }",
            "```",
            "",
            "## Exemplo (Python — `anthropic`)",
            "",
            "```python",
            "import anthropic",
            "",
            "client = anthropic.Anthropic()  # lê ANTHROPIC_API_KEY do ambiente",
            "",
            "message = client.messages.create(",
            "    model=\"<id-do-modelo-da-doc-oficial>\",",
            "    max_tokens=1024,",
            "    system=\"Você é um tutor de certificações Anthropic. Responda em pt-BR.\",",
            "    messages=[{\"role\": \"user\", \"content\": \"Explique o parâmetro max_tokens.\"}],",
            ")",
            "print(message.content)",
            "print(message.usage)",
            "```",
            "",
            "## A resposta (`Message`)",
            "",
            "- **`content`** — lista de *content blocks* (normalmente um bloco `text`; pode conter `tool_use`).",
            "- **`stop_reason`** — `\"end_turn\"`, `\"max_tokens\"`, `\"stop_sequence\"` ou `\"tool_use\"`.",
            "- **`usage`** — `input_tokens` e `output_tokens` (base do cálculo de custo).",
            "",
            "## Parâmetros de amostragem",
            "",
            "- **`temperature`** — baixa (perto de 0) para tarefas factuais/determinísticas; alta para diversidade.",
            "- **`top_p`** — nucleus sampling; geralmente ajuste **um** entre temperature e top_p.",
            "- **`stop_sequences`** — strings que, quando geradas, encerram a resposta.",
            "",
            "## Escolha de tier",
            "",
            "Comece simples: muitas tarefas vão bem em **Sonnet**. Suba para **Opus** quando o raciocínio for difícil; desça para **Haiku** quando o volume for alto e a tarefa, simples. Sempre meça antes de decidir.",
            "",
            "> Leitura oficial: https://docs.anthropic.com/en/api/messages",
          ].join("\n"),
          flashcards: [
            {
              front: "O que `max_tokens` limita na Messages API?",
              back: "Apenas a SAÍDA gerada pelo modelo (não a entrada). É obrigatório. Se o teto é atingido, stop_reason = \"max_tokens\".",
              tags: ["api"],
            },
            {
              front: "Onde vai o system prompt?",
              back: "No campo de topo `system`, fora do array `messages`. Define papel/regras/formato; não é um turno de usuário.",
              tags: ["api"],
            },
            {
              front: "Quais são os valores possíveis de `stop_reason`?",
              back: "\"end_turn\", \"max_tokens\", \"stop_sequence\" e \"tool_use\".",
              tags: ["api"],
            },
            {
              front: "Como decidir entre Opus, Sonnet e Haiku?",
              back: "Comece por Sonnet (equilíbrio). Suba para Opus em raciocínio difícil; use Haiku em alto volume/baixa complexidade. Meça antes de fixar.",
              tags: ["api"],
            },
          ],
          questions: [
            {
              prompt: "Qual é a regra dos papéis (roles) no array `messages`?",
              options: [
                "Podem aparecer em qualquer ordem",
                "Devem alternar user/assistant, começando por user",
                "Devem começar por assistant",
                "Só pode haver um turno user",
              ],
              correctIndex: 1,
              explanation:
                "Os papéis user e assistant se alternam e a conversa começa com um turno user.",
              difficulty: 2,
              tags: ["api"],
            },
            {
              prompt:
                "Para uma extração factual que precisa de respostas consistentes, qual ajuste faz sentido?",
              options: [
                "temperature alta",
                "temperature baixa (perto de 0)",
                "remover max_tokens",
                "usar dois turnos assistant seguidos",
              ],
              correctIndex: 1,
              explanation:
                "Temperatura baixa reduz aleatoriedade, dando saídas mais determinísticas — ideal para extração/classificação.",
              difficulty: 1,
              tags: ["api"],
            },
            {
              prompt: "O que há em `usage` na resposta?",
              options: [
                "O system prompt",
                "input_tokens e output_tokens",
                "A lista de ferramentas",
                "O stop_reason",
              ],
              correctIndex: 1,
              explanation:
                "usage traz input_tokens e output_tokens, base do cálculo de custo.",
              difficulty: 1,
              tags: ["api"],
            },
            {
              prompt: "Como o cliente normalmente recebe a API key nos SDKs oficiais?",
              options: [
                "Hardcoded no código-fonte",
                "Pela variável de ambiente ANTHROPIC_API_KEY",
                "Como query string na URL",
                "Não é necessária",
              ],
              correctIndex: 1,
              explanation:
                "Tanto `new Anthropic()` (TS) quanto `anthropic.Anthropic()` (Python) leem ANTHROPIC_API_KEY do ambiente por padrão. Nunca faça hardcode de chaves.",
              difficulty: 1,
              tags: ["api", "seguranca"],
            },
          ],
          labs: [
            {
              title: "Lab: primeiro endpoint de chat com system prompt e usage",
              promptMd: [
                "## Objetivo",
                "",
                "Escreva uma função (TypeScript **ou** Python) que receba uma pergunta do usuário e chame a Messages API uma única vez, com:",
                "",
                "1. um **system prompt** definindo o papel \"tutor de certificações Anthropic, responde em pt-BR\";",
                "2. `max_tokens` adequado;",
                "3. retorno do **texto** da resposta E do **usage** (input/output tokens).",
                "",
                "Explique, em comentário, por que você não fixou um preço no código e como escolheria o tier (Opus/Sonnet/Haiku).",
              ].join("\n"),
              rubric: [
                "Usa o campo de topo `system` (não um role system dentro de messages)",
                "Define `max_tokens` e justifica que ele limita só a saída",
                "Extrai e expõe `usage` (input_tokens/output_tokens)",
                "Lê a API key do ambiente (ANTHROPIC_API_KEY), sem hardcode",
                "Raciocina por tier e remete o ID/preço exato à doc oficial",
              ],
              modelAnswer: [
                "```ts",
                "import Anthropic from \"@anthropic-ai/sdk\";",
                "",
                "const client = new Anthropic(); // ANTHROPIC_API_KEY vem do ambiente",
                "",
                "export async function perguntar(pergunta: string) {",
                "  const msg = await client.messages.create({",
                "    // Tier Sonnet costuma equilibrar custo/latência/capacidade para tutoria.",
                "    // Use o ID exato da tabela oficial de modelos (muda com o tempo).",
                "    model: \"<id-do-modelo-da-doc-oficial>\",",
                "    max_tokens: 1024, // limita só a SAÍDA gerada",
                "    system: \"Você é um tutor de certificações Anthropic. Responda em pt-BR, claro e conciso.\",",
                "    messages: [{ role: \"user\", content: pergunta }],",
                "  });",
                "",
                "  const texto = msg.content",
                "    .filter((b) => b.type === \"text\")",
                "    .map((b) => (b as { text: string }).text)",
                "    .join(\"\");",
                "",
                "  // Não fixamos preço no código: ele muda. usage permite calcular o custo",
                "  // multiplicando por tarifas que você lê da doc oficial em runtime/config.",
                "  return { texto, usage: msg.usage };",
                "}",
                "```",
                "",
                "Escolha de tier: comece em Sonnet; suba para Opus se as respostas exigirem raciocínio mais difícil; use Haiku se for alto volume e baixa complexidade. Sempre meça com evals antes de fixar.",
              ].join("\n"),
            },
          ],
        },
        {
          title: "SDKs, autenticação e resiliência básica",
          readingMd: [
            "# SDKs, autenticação e resiliência básica",
            "",
            "## SDKs oficiais",
            "",
            "- **TypeScript/JavaScript**: pacote npm **`@anthropic-ai/sdk`** (`import Anthropic from \"@anthropic-ai/sdk\"`).",
            "- **Python**: pacote **`anthropic`** (`import anthropic`).",
            "",
            "Ambos expõem `client.messages.create(...)` e, para respostas longas, helpers de streaming (`client.messages.stream(...)`).",
            "",
            "## Autenticação",
            "",
            "A chave é uma **API key** enviada no header `x-api-key` (os SDKs fazem isso por você). Por padrão, o cliente lê **`ANTHROPIC_API_KEY`** do ambiente. **Regras de ouro de segurança:**",
            "",
            "- nunca faça hardcode da chave no código nem a versione no Git;",
            "- carregue de variável de ambiente / secret manager;",
            "- a chave é um segredo de **backend** — não a exponha no frontend.",
            "",
            "## `request-id` para depuração",
            "",
            "Toda resposta inclui o header **`request-id`** (algo como `req_018Ee...`). Os SDKs expõem isso no objeto de resposta (ex.: `message._request_id`). Ao abrir um chamado de suporte, **inclua o request-id** — é o que identifica sua requisição.",
            "",
            "## Streaming para respostas longas",
            "",
            "Para gerações longas, prefira **streaming** (SSE). Sem streaming, conexões ociosas podem cair em algumas redes e a requisição falha por timeout. Os SDKs validam que requisições não-streaming não devem passar de ~10 minutos. Em TypeScript, `client.messages.stream(...)` com `.finalMessage()` dá o `Message` completo sem você tratar cada evento; em Python, `with client.messages.stream(...) as stream: stream.get_final_message()`.",
            "",
            "```ts",
            "const stream = client.messages.stream({",
            "  model: \"<id-do-modelo-da-doc-oficial>\",",
            "  max_tokens: 4096,",
            "  messages: [{ role: \"user\", content: \"Escreva uma análise detalhada...\" }],",
            "});",
            "const message = await stream.finalMessage();",
            "```",
            "",
            "## Por que isso importa para um desenvolvedor",
            "",
            "Autenticação correta + leitura do `request-id` + streaming nos lugares certos formam a base de uma integração de produção saudável. Erros e retries são aprofundados no Módulo 4.",
            "",
            "> Leitura oficial: https://docs.anthropic.com/en/api/client-sdks",
          ].join("\n"),
          flashcards: [
            {
              front: "Quais são os nomes dos pacotes oficiais (TS e Python)?",
              back: "TypeScript: `@anthropic-ai/sdk`. Python: `anthropic`.",
              tags: ["sdk"],
            },
            {
              front: "Onde a API key NÃO deve estar?",
              back: "Hardcoded no código, versionada no Git ou exposta no frontend. Ela é um segredo de backend; carregue de env/secret manager.",
              tags: ["sdk", "seguranca"],
            },
            {
              front: "Para que serve o header `request-id`?",
              back: "Identifica unicamente a requisição. Inclua-o ao abrir chamados de suporte. Os SDKs o expõem (ex.: message._request_id).",
              tags: ["sdk"],
            },
            {
              front: "Quando usar streaming?",
              back: "Em respostas longas: evita que conexões ociosas caiam e o request falhe por timeout. SDKs alertam para não exceder ~10 min em requisições não-streaming.",
              tags: ["sdk"],
            },
          ],
          questions: [
            {
              prompt: "Qual é o pacote npm do SDK oficial em TypeScript?",
              options: ["anthropic-js", "@anthropic-ai/sdk", "claude-sdk", "@claude/api"],
              correctIndex: 1,
              explanation: "O SDK oficial em TypeScript é `@anthropic-ai/sdk`.",
              difficulty: 1,
              tags: ["sdk"],
            },
            {
              prompt: "Qual prática de segurança está CORRETA para a API key?",
              options: [
                "Colocar a chave no bundle do frontend para reduzir latência",
                "Hardcode no código e commitar no Git",
                "Carregar de variável de ambiente no backend, sem expor ao cliente",
                "Enviar a chave na query string da URL",
              ],
              correctIndex: 2,
              explanation:
                "A chave é segredo de backend. Carregue de env/secret manager; nunca exponha no frontend nem versione no Git.",
              difficulty: 1,
              tags: ["sdk", "seguranca"],
            },
            {
              prompt: "Por que preferir streaming em respostas muito longas?",
              options: [
                "Porque reduz o número de tokens cobrados",
                "Porque evita timeouts de conexões ociosas em gerações longas",
                "Porque dispensa a API key",
                "Porque aumenta o max_tokens automaticamente",
              ],
              correctIndex: 1,
              explanation:
                "Streaming mantém a conexão ativa e entrega tokens incrementalmente, evitando quedas por timeout em respostas longas.",
              difficulty: 2,
              tags: ["sdk"],
            },
          ],
        },
      ],
    },

    // =====================================================================
    // MÓDULO 2 — CONSTRUINDO AGENTES
    // =====================================================================
    {
      title: "Construindo agentes",
      lessons: [
        {
          title: "O que é um agente: o LLM aumentado e o loop de tool use",
          readingMd: [
            "# O que é um agente: o LLM aumentado e o loop de tool use",
            "",
            "## O bloco fundamental: o LLM aumentado",
            "",
            "A peça base de qualquer sistema agêntico é o **LLM aumentado** — um modelo com **recuperação (retrieval)**, **ferramentas (tools)** e **memória**. O modelo gera consultas de busca, escolhe ferramentas e decide o que reter. Tudo o mais é orquestração ao redor dessa peça.",
            "",
            "## Tool use: como Claude chama funções",
            "",
            "Você descreve ferramentas no parâmetro **`tools`**. Cada **client tool** (executada no SEU código) tem:",
            "",
            "- **`name`** — identificador da função;",
            "- **`description`** — descrição clara do que ela faz e quando usar (isto é o que mais influencia o acerto do modelo);",
            "- **`input_schema`** — JSON Schema dos parâmetros.",
            "",
            "Quando Claude decide usar uma ferramenta, a resposta vem com **`stop_reason: \"tool_use\"`** e um ou mais blocos **`tool_use`** (com `id`, `name`, `input`). Seu código executa e devolve um bloco **`tool_result`** (referenciando o `tool_use_id`) num novo turno `user`. O modelo então continua.",
            "",
            "Há também **server tools** (ex.: `web_search`, `code_execution`), executadas pela infraestrutura da Anthropic — você recebe o resultado sem orquestrar a execução.",
            "",
            "## O loop de tool use (o coração do agente)",
            "",
            "1. Envie a mensagem do usuário **+** as definições de `tools`.",
            "2. Se `stop_reason == \"tool_use\"`: para cada bloco `tool_use`, execute a função e colete o resultado.",
            "3. Reenvie o histórico **incluindo** os `tool_result`.",
            "4. Repita até `stop_reason == \"end_turn\"` (resposta final).",
            "",
            "```ts",
            "import Anthropic from \"@anthropic-ai/sdk\";",
            "const client = new Anthropic();",
            "",
            "const tools = [{",
            "  name: \"get_weather\",",
            "  description: \"Retorna o clima atual de uma cidade. Use quando o usuário perguntar sobre tempo/clima.\",",
            "  input_schema: {",
            "    type: \"object\",",
            "    properties: { location: { type: \"string\", description: \"Cidade, ex.: 'São Paulo'\" } },",
            "    required: [\"location\"],",
            "  },",
            "}];",
            "",
            "let messages = [{ role: \"user\", content: \"Qual o clima em São Paulo?\" }];",
            "let res = await client.messages.create({ model: \"<id>\", max_tokens: 1024, tools, messages });",
            "",
            "while (res.stop_reason === \"tool_use\") {",
            "  const toolResults = [];",
            "  for (const block of res.content) {",
            "    if (block.type === \"tool_use\") {",
            "      const out = await executar(block.name, block.input); // SEU código",
            "      toolResults.push({ type: \"tool_result\", tool_use_id: block.id, content: out });",
            "    }",
            "  }",
            "  messages = [...messages, { role: \"assistant\", content: res.content }, { role: \"user\", content: toolResults }];",
            "  res = await client.messages.create({ model: \"<id>\", max_tokens: 1024, tools, messages });",
            "}",
            "```",
            "",
            "## `tool_choice`: nudge vs. garantia",
            "",
            "- **`{ type: \"auto\" }`** (padrão) — Claude decide a cada turno se chama ferramenta ou responde direto. Dá pra reforçar via system prompt (\"Use as ferramentas para investigar antes de responder\").",
            "- **`{ type: \"any\" }`** — força usar **alguma** ferramenta.",
            "- **`{ type: \"tool\", name }`** — força uma ferramenta específica.",
            "- **`{ type: \"none\" }`** — proíbe ferramentas.",
            "",
            "Para uma garantia rígida, use `tool_choice`; para um empurrão suave, ajuste a descrição/system prompt. Dica: `strict: true` na definição garante conformidade exata ao schema.",
            "",
            "> Leitura oficial: https://docs.anthropic.com/en/docs/build-with-claude/tool-use/overview",
          ].join("\n"),
          flashcards: [
            {
              front: "O que é o \"LLM aumentado\"?",
              back: "O bloco base de sistemas agênticos: um LLM com recuperação (retrieval), ferramentas (tools) e memória. Toda orquestração de agentes se constrói ao redor dele.",
              tags: ["agentes"],
            },
            {
              front: "Quais três campos definem uma client tool?",
              back: "`name`, `description` (a parte mais decisiva para o acerto) e `input_schema` (JSON Schema dos parâmetros).",
              tags: ["tool-use"],
            },
            {
              front: "Qual stop_reason indica que Claude quer chamar uma ferramenta?",
              back: "`stop_reason: \"tool_use\"`. A resposta traz blocos `tool_use` (id, name, input); você executa e devolve `tool_result` com o tool_use_id.",
              tags: ["tool-use"],
            },
            {
              front: "Diferença entre client tools e server tools?",
              back: "Client tools rodam no SEU código (você executa e devolve tool_result). Server tools (ex.: web_search, code_execution) rodam na infraestrutura da Anthropic; você recebe o resultado pronto.",
              tags: ["tool-use"],
            },
          ],
          questions: [
            {
              prompt:
                "No loop de tool use, o que você faz quando `stop_reason == \"tool_use\"`?",
              options: [
                "Encerra a conversa imediatamente",
                "Executa a(s) ferramenta(s) e reenvia o histórico com os blocos tool_result",
                "Ignora o bloco tool_use e responde ao usuário",
                "Reduz max_tokens para 0",
              ],
              correctIndex: 1,
              explanation:
                "Você executa cada tool_use, coleta a saída em tool_result (com o tool_use_id) e reenvia. O modelo então continua até end_turn.",
              difficulty: 2,
              tags: ["tool-use"],
            },
            {
              prompt: "Qual campo de uma ferramenta MAIS influencia se o modelo a chama corretamente?",
              options: ["O `name`", "A `description`", "O `tool_use_id`", "O `max_tokens`"],
              correctIndex: 1,
              explanation:
                "Uma `description` clara (o que faz e quando usar) é o que mais melhora a decisão do modelo de chamar a ferramenta certa.",
              difficulty: 2,
              tags: ["tool-use"],
            },
            {
              prompt:
                "Você precisa GARANTIR que o modelo use alguma ferramenta nesta chamada. O que usar?",
              options: [
                "tool_choice { type: \"auto\" }",
                "tool_choice { type: \"none\" }",
                "tool_choice { type: \"any\" } (ou { type: \"tool\", name } para uma específica)",
                "Remover o parâmetro tools",
              ],
              correctIndex: 2,
              explanation:
                "`any` força usar alguma ferramenta; `tool` força uma específica. `auto` apenas deixa o modelo decidir; `none` proíbe.",
              difficulty: 2,
              tags: ["tool-use"],
            },
            {
              prompt: "O que `strict: true` em uma definição de ferramenta garante?",
              options: [
                "Que a ferramenta sempre será chamada",
                "Que as chamadas do modelo conformam exatamente ao seu JSON Schema",
                "Que a execução roda na infraestrutura da Anthropic",
                "Que o resultado é cacheado",
              ],
              correctIndex: 1,
              explanation:
                "Strict tool use força as chamadas a baterem exatamente com o input_schema declarado.",
              difficulty: 3,
              tags: ["tool-use"],
            },
          ],
          labs: [
            {
              title: "Lab: implemente um loop de tool use completo",
              promptMd: [
                "## Objetivo",
                "",
                "Implemente (TS ou Python) um agente mínimo com **uma** client tool `get_weather(location)` e o **loop de tool use** completo:",
                "",
                "1. defina a tool com `name`, `description` e `input_schema`;",
                "2. chame a Messages API com `tools`;",
                "3. enquanto `stop_reason == \"tool_use\"`, execute a função (pode ser um mock) e devolva `tool_result` com o `tool_use_id` correto;",
                "4. pare quando `stop_reason == \"end_turn\"` e retorne o texto final.",
                "",
                "Trate o caso de **parâmetro ausente** (o usuário não informou a cidade).",
              ].join("\n"),
              rubric: [
                "Define a tool com name, description clara e input_schema válido",
                "Implementa o loop: detecta stop_reason tool_use e itera",
                "Devolve tool_result referenciando o tool_use_id correto",
                "Reenvia o histórico (assistant content + user tool_result) a cada iteração",
                "Encerra em end_turn e considera o caso de parâmetro ausente",
              ],
              modelAnswer: [
                "```python",
                "import anthropic",
                "client = anthropic.Anthropic()",
                "",
                "tools = [{",
                "    \"name\": \"get_weather\",",
                "    \"description\": \"Retorna o clima atual de uma cidade. Use ao perguntarem sobre tempo/clima.\",",
                "    \"input_schema\": {",
                "        \"type\": \"object\",",
                "        \"properties\": {\"location\": {\"type\": \"string\", \"description\": \"Cidade\"}},",
                "        \"required\": [\"location\"],",
                "    },",
                "}]",
                "",
                "def executar(name, args):",
                "    if name == \"get_weather\":",
                "        # mock; em produção chamaria uma API real",
                "        return f\"Clima em {args['location']}: 24C, ensolarado.\"",
                "    return \"ferramenta desconhecida\"",
                "",
                "def agente(pergunta: str):",
                "    messages = [{\"role\": \"user\", \"content\": pergunta}]",
                "    res = client.messages.create(model=\"<id>\", max_tokens=1024, tools=tools, messages=messages)",
                "    while res.stop_reason == \"tool_use\":",
                "        tool_results = []",
                "        for block in res.content:",
                "            if block.type == \"tool_use\":",
                "                out = executar(block.name, block.input)",
                "                tool_results.append({",
                "                    \"type\": \"tool_result\",",
                "                    \"tool_use_id\": block.id,",
                "                    \"content\": out,",
                "                })",
                "        messages.append({\"role\": \"assistant\", \"content\": res.content})",
                "        messages.append({\"role\": \"user\", \"content\": tool_results})",
                "        res = client.messages.create(model=\"<id>\", max_tokens=1024, tools=tools, messages=messages)",
                "    return \"\".join(b.text for b in res.content if b.type == \"text\")",
                "```",
                "",
                "Parâmetro ausente: se o usuário não dá a cidade, Claude (sobretudo o tier Opus) tende a PERGUNTAR em vez de chamar a tool. Você não precisa forçar — responda à pergunta de clarificação e siga o loop.",
              ].join("\n"),
            },
          ],
        },
        {
          title: "Workflows vs. agentes: padrões e quando usar cada um",
          readingMd: [
            "# Workflows vs. agentes: padrões e quando usar cada um",
            "",
            "## A distinção central",
            "",
            "- **Workflows** — LLMs e ferramentas orquestrados por **caminhos de código predefinidos**. Você controla o fluxo. Previsível, fácil de depurar.",
            "- **Agentes** — o LLM **dirige dinamicamente** seu próprio processo e uso de ferramentas, decidindo os passos. Flexível, porém mais imprevisível, caro e difícil de depurar.",
            "",
            "**Regra de ouro:** comece simples. Otimize uma única chamada de LLM (com retrieval e exemplos no contexto) antes de adicionar complexidade. Só suba para um sistema agêntico quando o ganho de qualidade **justificar** a latência e o custo extras.",
            "",
            "## Padrões de workflow",
            "",
            "1. **Prompt chaining** — decompõe a tarefa em passos sequenciais; a saída de um vira entrada do próximo, com checkpoints programáticos entre eles.",
            "2. **Routing** — classifica a entrada e a direciona a um caminho especializado (ex.: triagem de tickets).",
            "3. **Parallelization** — roda várias chamadas em paralelo. Duas formas: **sectioning** (subtarefas independentes) e **voting** (mesma tarefa várias vezes para votar/diversificar).",
            "4. **Orchestrator-workers** — um LLM orquestrador quebra a tarefa dinamicamente e delega a workers, depois sintetiza.",
            "5. **Evaluator-optimizer** — um LLM gera, outro avalia e dá feedback num loop de refinamento.",
            "",
            "## O agente autônomo",
            "",
            "Quando o problema é **aberto** e o número de passos é imprevisível, use um **agente autônomo**: ele opera em loop com ferramentas, usando o **feedback do ambiente** (resultados das tools) para decidir o próximo passo. Cuidados essenciais:",
            "",
            "- **pare claramente** (limite de iterações / condição de parada) para evitar loops infinitos;",
            "- dê **ferramentas bem documentadas** (a interface agente↔ferramenta é tão importante quanto a interface humano↔computador);",
            "- prefira ambientes onde o agente pode **verificar o próprio trabalho** (testes, validações).",
            "",
            "## Como escolher",
            "",
            "| Situação | Escolha |",
            "| --- | --- |",
            "| Passos fixos e conhecidos | Workflow (chaining/routing) |",
            "| Subtarefas independentes | Parallelization (sectioning) |",
            "| Precisa de robustez/diversidade | Parallelization (voting) |",
            "| Decomposição imprevisível | Orchestrator-workers |",
            "| Refinamento iterativo com critério claro | Evaluator-optimizer |",
            "| Problema aberto, muitos passos | Agente autônomo |",
            "",
            "> Leitura oficial: https://www.anthropic.com/engineering/building-effective-agents",
          ].join("\n"),
          flashcards: [
            {
              front: "Workflow vs. agente: qual a diferença?",
              back: "Workflow = LLM/ferramentas orquestrados por caminhos de código predefinidos (você controla o fluxo). Agente = o LLM dirige dinamicamente seus próprios passos e uso de ferramentas.",
              tags: ["agentes"],
            },
            {
              front: "Qual a \"regra de ouro\" antes de construir um agente?",
              back: "Comece simples: otimize uma única chamada (com retrieval/exemplos) primeiro. Só adicione complexidade agêntica quando o ganho justificar a latência e o custo extras.",
              tags: ["agentes"],
            },
            {
              front: "Diferença entre sectioning e voting na parallelization?",
              back: "Sectioning = dividir em subtarefas independentes rodadas em paralelo. Voting = rodar a MESMA tarefa várias vezes para votar/diversificar resultados.",
              tags: ["agentes"],
            },
            {
              front: "O que difere orchestrator-workers de routing?",
              back: "Routing classifica e manda a um caminho fixo. Orchestrator-workers decompõe dinamicamente uma tarefa imprevisível e delega a workers, depois sintetiza.",
              tags: ["agentes"],
            },
          ],
          questions: [
            {
              prompt: "Quando um agente autônomo é a melhor escolha?",
              options: [
                "Quando os passos são fixos e conhecidos de antemão",
                "Quando o problema é aberto, com número de passos imprevisível",
                "Sempre, pois é mais barato que um workflow",
                "Nunca; agentes não devem ser usados em produção",
              ],
              correctIndex: 1,
              explanation:
                "Agentes autônomos brilham em problemas abertos e multi-passo imprevisíveis. Para passos fixos, workflows são mais simples e previsíveis.",
              difficulty: 2,
              tags: ["agentes"],
            },
            {
              prompt:
                "Você tem triagem de tickets: cada ticket vai para um fluxo especializado. Qual padrão?",
              options: ["Voting", "Routing", "Evaluator-optimizer", "Agente autônomo"],
              correctIndex: 1,
              explanation:
                "Routing classifica a entrada e a direciona ao caminho especializado adequado.",
              difficulty: 1,
              tags: ["agentes"],
            },
            {
              prompt:
                "Qual cuidado é ESSENCIAL ao construir um agente autônomo?",
              options: [
                "Remover todas as ferramentas",
                "Definir condição/limite de parada para evitar loops infinitos",
                "Usar sempre temperature 1",
                "Proibir verificação do próprio trabalho",
              ],
              correctIndex: 1,
              explanation:
                "Sem condição de parada (limite de iterações), um agente pode entrar em loop. Ferramentas bem documentadas e verificação do trabalho também são chave.",
              difficulty: 2,
              tags: ["agentes"],
            },
            {
              prompt:
                "Por que NÃO partir direto para um agente autônomo em toda tarefa?",
              options: [
                "Porque agentes não conseguem usar ferramentas",
                "Porque adicionam latência, custo e imprevisibilidade que nem sempre se justificam",
                "Porque a API não suporta tool use",
                "Porque workflows não podem usar LLMs",
              ],
              correctIndex: 1,
              explanation:
                "A orientação oficial é começar simples; complexidade agêntica só quando o ganho de qualidade compensa o custo e a latência adicionais.",
              difficulty: 1,
              tags: ["agentes"],
            },
          ],
          labs: [
            {
              title: "Lab: escolha e justifique um padrão de agente",
              promptMd: [
                "## Objetivo",
                "",
                "Para CADA cenário abaixo, escolha o padrão mais adequado (prompt chaining, routing, parallelization-sectioning, parallelization-voting, orchestrator-workers, evaluator-optimizer ou agente autônomo) e justifique em 1-2 frases:",
                "",
                "1. Gerar um post: rascunhar → revisar tom → revisar gramática (passos fixos).",
                "2. Suporte: classificar a mensagem e mandar ao time certo.",
                "3. Traduzir 1 documento e, em paralelo, extrair entidades dele.",
                "4. Pesquisa aberta sobre um tema, com número de buscas imprevisível.",
                "5. Gerar código e refiná-lo até passar nos testes.",
              ].join("\n"),
              rubric: [
                "Cenário 1 → prompt chaining (passos sequenciais com checkpoints)",
                "Cenário 2 → routing (classifica e direciona)",
                "Cenário 3 → parallelization/sectioning (subtarefas independentes)",
                "Cenário 4 → agente autônomo (problema aberto, passos imprevisíveis)",
                "Cenário 5 → evaluator-optimizer (gera e refina com feedback até critério)",
                "Cada escolha vem com justificativa coerente (custo/latência/previsibilidade)",
              ],
              modelAnswer: [
                "1. **Prompt chaining** — passos fixos e sequenciais; cada etapa refina a saída da anterior, com checkpoints entre elas.",
                "2. **Routing** — a tarefa é classificar e direcionar; cada classe segue um fluxo especializado.",
                "3. **Parallelization (sectioning)** — tradução e extração são subtarefas independentes; rodá-las em paralelo reduz latência.",
                "4. **Agente autônomo** — problema aberto: o número de buscas depende do que for descoberto; o agente decide os passos com base no feedback do ambiente (com limite de iterações).",
                "5. **Evaluator-optimizer** — há critério objetivo (testes passarem); um LLM gera, outro avalia e dá feedback em loop até o critério ser atingido.",
              ].join("\n"),
            },
          ],
        },
        {
          title: "Claude Agent SDK: agentes prontos para produção",
          readingMd: [
            "# Claude Agent SDK: agentes prontos para produção",
            "",
            "O **Claude Agent SDK** dá o mesmo agent loop, ferramentas embutidas e gestão de contexto que alimentam o Claude Code — programável em **TypeScript** e **Python**. Em vez de você implementar o loop de tool use à mão (como no Módulo 2.1), o SDK já o executa.",
            "",
            "## Pacotes",
            "",
            "- **TypeScript**: `npm install @anthropic-ai/claude-agent-sdk` → `import { query } from \"@anthropic-ai/claude-agent-sdk\";`",
            "- **Python**: `pip install claude-agent-sdk` (requer Python 3.10+) → `from claude_agent_sdk import query, ClaudeAgentOptions`",
            "",
            "Autentica via `ANTHROPIC_API_KEY` (ou provedores como Bedrock/Vertex/Azure por variáveis de ambiente).",
            "",
            "## Exemplo mínimo",
            "",
            "```ts",
            "import { query } from \"@anthropic-ai/claude-agent-sdk\";",
            "",
            "for await (const message of query({",
            "  prompt: \"Encontre e corrija o bug em auth.ts\",",
            "  options: { allowedTools: [\"Read\", \"Edit\", \"Bash\"] },",
            "})) {",
            "  console.log(message);",
            "}",
            "```",
            "",
            "```python",
            "import asyncio",
            "from claude_agent_sdk import query, ClaudeAgentOptions",
            "",
            "async def main():",
            "    async for message in query(",
            "        prompt=\"Encontre e corrija o bug em auth.py\",",
            "        options=ClaudeAgentOptions(allowed_tools=[\"Read\", \"Edit\", \"Bash\"]),",
            "    ):",
            "        print(message)",
            "",
            "asyncio.run(main())",
            "```",
            "",
            "## Agent SDK vs. Client SDK",
            "",
            "- **Client SDK** (`@anthropic-ai/sdk` / `anthropic`): acesso direto à API; **você** implementa o loop de tool use.",
            "- **Agent SDK**: Claude executa o loop e as ferramentas embutidas por você. Ideal quando o agente precisa ler arquivos, rodar comandos e editar código.",
            "",
            "## Capacidades-chave",
            "",
            "- **Ferramentas embutidas**: `Read`, `Write`, `Edit`, `Bash`, `Glob`, `Grep`, `WebSearch`, `WebFetch`, `AskUserQuestion`, etc.",
            "- **Permissions**: controle fino com `allowedTools` (pré-aprova ferramentas seguras). Para escrita, há modos como `permissionMode: \"acceptEdits\"`.",
            "- **Hooks**: rode código em pontos do ciclo de vida — `PreToolUse`, `PostToolUse`, `Stop`, `SessionStart`, `SessionEnd`, `UserPromptSubmit` — para validar, logar, bloquear ou transformar comportamento (ótimo para auditoria/observabilidade).",
            "- **Subagents**: agentes especializados invocados via a ferramenta `Agent`; o agente principal delega e recebe o resultado. Inclua `Agent` em `allowedTools` para auto-aprovar.",
            "- **MCP**: conecte sistemas externos (bancos, browsers, APIs) via Model Context Protocol com `mcpServers`.",
            "- **Sessions**: mantenha contexto entre chamadas; capture o `session_id` na mensagem de init e use `resume` para continuar.",
            "",
            "## Agent SDK vs. Managed Agents",
            "",
            "O Agent SDK roda o loop **no seu processo/infra**. Os **Managed Agents** são uma API REST hospedada: a Anthropic roda o agente e um sandbox por sessão. Caminho comum: prototipar local com o Agent SDK e migrar para Managed Agents em produção quando não quiser operar sandbox/infra de sessão.",
            "",
            "> Leitura oficial: https://docs.anthropic.com/en/docs/agents-and-tools/agent-sdk/overview",
          ].join("\n"),
          flashcards: [
            {
              front: "Quais os pacotes do Claude Agent SDK (TS e Python)?",
              back: "TypeScript: `@anthropic-ai/claude-agent-sdk`. Python: `claude-agent-sdk` (requer Python 3.10+).",
              tags: ["agent-sdk", "sdk"],
            },
            {
              front: "Diferença prática entre Agent SDK e Client SDK?",
              back: "No Client SDK você implementa o loop de tool use. No Agent SDK, Claude executa o loop e as ferramentas embutidas por você (Read/Edit/Bash etc.).",
              tags: ["agent-sdk"],
            },
            {
              front: "Para que servem os hooks do Agent SDK?",
              back: "Rodar código em pontos do ciclo de vida (PreToolUse, PostToolUse, Stop, SessionStart/End, UserPromptSubmit) para validar, logar, bloquear ou transformar o comportamento do agente.",
              tags: ["agent-sdk", "observabilidade"],
            },
            {
              front: "Como manter contexto entre chamadas no Agent SDK?",
              back: "Via sessions: capture o session_id (mensagem de init) e passe `resume: sessionId` na próxima query para continuar com todo o contexto.",
              tags: ["agent-sdk"],
            },
          ],
          questions: [
            {
              prompt: "Qual é o pacote do Agent SDK em Python?",
              options: ["anthropic", "claude-agent-sdk", "@anthropic-ai/claude-agent-sdk", "claude-code"],
              correctIndex: 1,
              explanation:
                "Em Python é `claude-agent-sdk` (pip). Em TypeScript é `@anthropic-ai/claude-agent-sdk` (npm). `anthropic` é o Client SDK.",
              difficulty: 1,
              tags: ["agent-sdk"],
            },
            {
              prompt:
                "Você quer um agente que lê arquivos e roda comandos sem você escrever o loop de tool use. Qual escolher?",
              options: [
                "Client SDK (@anthropic-ai/sdk)",
                "Agent SDK (executa o loop e as ferramentas embutidas)",
                "Apenas a Messages API via cURL",
                "Nenhum suporta isso",
              ],
              correctIndex: 1,
              explanation:
                "O Agent SDK já traz o agent loop e ferramentas embutidas (Read/Edit/Bash), evitando implementar o loop manualmente.",
              difficulty: 2,
              tags: ["agent-sdk"],
            },
            {
              prompt: "Como pré-aprovar quais ferramentas o agente pode usar?",
              options: [
                "Com o parâmetro `temperature`",
                "Com `allowedTools` / `allowed_tools` nas options",
                "Removendo o session_id",
                "Não é possível restringir ferramentas",
              ],
              correctIndex: 1,
              explanation:
                "`allowedTools` (TS) / `allowed_tools` (Python) define o conjunto pré-aprovado de ferramentas — base do modelo de permissões.",
              difficulty: 2,
              tags: ["agent-sdk", "seguranca"],
            },
            {
              prompt: "Qual a diferença entre Agent SDK e Managed Agents?",
              options: [
                "São o mesmo produto com nomes diferentes",
                "Agent SDK roda o loop no seu processo/infra; Managed Agents é uma API REST hospedada com sandbox por sessão",
                "Managed Agents só existe em TypeScript",
                "Agent SDK não suporta MCP",
              ],
              correctIndex: 1,
              explanation:
                "Agent SDK = biblioteca que roda no seu processo. Managed Agents = serviço hospedado onde a Anthropic opera o agente e o sandbox.",
              difficulty: 3,
              tags: ["agent-sdk"],
            },
          ],
          labs: [
            {
              title: "Lab: agente de tarefa única com Agent SDK e permissões mínimas",
              promptMd: [
                "## Objetivo",
                "",
                "Esboce (TS ou Python) um agente com o **Agent SDK** que: (a) recebe um prompt para resumir todos os TODOs do código; (b) usa **apenas** ferramentas de leitura (`Read`, `Glob`, `Grep`) — sem escrita; (c) registra cada uso de ferramenta num log de auditoria via **hook** `PostToolUse`.",
                "",
                "Justifique por que limitar `allowedTools` a leitura é uma boa prática de segurança.",
              ].join("\n"),
              rubric: [
                "Importa `query` do pacote correto (claude-agent-sdk / @anthropic-ai/claude-agent-sdk)",
                "Define allowedTools apenas com ferramentas de leitura (Read/Glob/Grep)",
                "Configura um hook PostToolUse para auditoria",
                "Itera sobre as mensagens do agente e extrai o resultado",
                "Justifica o princípio de menor privilégio (read-only reduz risco)",
              ],
              modelAnswer: [
                "```python",
                "import asyncio",
                "from datetime import datetime",
                "from claude_agent_sdk import query, ClaudeAgentOptions, HookMatcher",
                "",
                "async def log_tool(input_data, tool_use_id, context):",
                "    with open(\"./audit.log\", \"a\") as f:",
                "        f.write(f\"{datetime.now()}: {input_data.get('tool_name')}\\n\")",
                "    return {}",
                "",
                "async def main():",
                "    async for message in query(",
                "        prompt=\"Encontre todos os comentários TODO e gere um resumo\",",
                "        options=ClaudeAgentOptions(",
                "            allowed_tools=[\"Read\", \"Glob\", \"Grep\"],  # somente leitura",
                "            hooks={\"PostToolUse\": [HookMatcher(matcher=\"*\", hooks=[log_tool])]},",
                "        ),",
                "    ):",
                "        if hasattr(message, \"result\"):",
                "            print(message.result)",
                "",
                "asyncio.run(main())",
                "```",
                "",
                "Princípio de menor privilégio: ao expor só Read/Glob/Grep, o agente NÃO pode editar, apagar ou rodar comandos arbitrários. Mesmo com um prompt malicioso ou uma alucinação, o pior caso é uma leitura indevida — não uma alteração destrutiva. O hook PostToolUse dá rastro de auditoria de tudo que foi acessado.",
              ].join("\n"),
            },
          ],
        },
      ],
    },

    // =====================================================================
    // MÓDULO 3 — RECUPERAÇÃO E DADOS (RAG)
    // =====================================================================
    {
      title: "Recuperação e dados (RAG)",
      lessons: [
        {
          title: "Embeddings e busca: encontrando o contexto certo",
          readingMd: [
            "# Embeddings e busca: encontrando o contexto certo",
            "",
            "**RAG (Retrieval-Augmented Generation)** = recuperar trechos relevantes de uma base de conhecimento e injetá-los no contexto do modelo, para que ele responda com base em **dados seus e atualizados**, reduzindo alucinação.",
            "",
            "## Por que RAG?",
            "",
            "- O modelo tem um **corte de conhecimento** e não conhece seus documentos privados.",
            "- Em vez de re-treinar, você **busca** o relevante e o coloca no prompt.",
            "- Bônus: a resposta pode **citar** as fontes (ver próxima lição).",
            "",
            "## Embeddings e busca semântica",
            "",
            "Um **embedding** é um vetor que representa o significado de um texto. Textos semanticamente próximos ficam próximos no espaço vetorial. A busca semântica compara o embedding da **pergunta** com os embeddings dos **trechos** (similaridade de cosseno) e retorna os mais próximos.",
            "",
            "> A Anthropic **não** oferece um modelo de embeddings de primeira parte; a doc recomenda provedores de embeddings de terceiros (ex.: Voyage AI e outros). Escolha o provedor pela doc oficial — não fixe nomes/preços de cor.",
            "",
            "## Pipeline típico de ingestão",
            "",
            "1. **Chunking** — quebre os documentos em trechos (ex.: por parágrafo/seção). Trechos pequenos demais perdem contexto; grandes demais diluem a relevância e gastam tokens.",
            "2. **Embed** — gere o embedding de cada trecho.",
            "3. **Index** — guarde os vetores num índice/vector store.",
            "4. **Retrieve** — na consulta, embed a pergunta, busque os top-k trechos.",
            "5. **Augment** — monte o prompt com os trechos + a pergunta.",
            "6. **Generate** — chame a Messages API.",
            "",
            "## Busca densa, esparsa e híbrida",
            "",
            "- **Densa (semântica)** — por embeddings; pega significado, mesmo com palavras diferentes.",
            "- **Esparsa (lexical, ex.: BM25)** — por palavras-chave; ótima para termos exatos, códigos, nomes próprios.",
            "- **Híbrida** — combina as duas e frequentemente **reranqueia** com um modelo dedicado. Costuma dar o melhor resultado.",
            "",
            "## Erros comuns",
            "",
            "- chunking ruim (cortar no meio de uma ideia);",
            "- recuperar trechos demais e estourar/diluir o contexto;",
            "- não reranquear quando a recuperação inicial é ruidosa;",
            "- esquecer de medir a qualidade da recuperação com um conjunto de avaliação (Módulo 4).",
            "",
            "> Leitura oficial: https://docs.anthropic.com/en/docs/build-with-claude/embeddings",
          ].join("\n"),
          flashcards: [
            {
              front: "O que é RAG e qual problema resolve?",
              back: "Retrieval-Augmented Generation: recupera trechos relevantes de uma base e os injeta no contexto. Resolve o corte de conhecimento e a falta de dados privados, reduzindo alucinação.",
              tags: ["rag"],
            },
            {
              front: "O que é um embedding?",
              back: "Um vetor que representa o significado de um texto. Textos semanticamente próximos ficam próximos no espaço; a busca usa similaridade (ex.: cosseno).",
              tags: ["embeddings"],
            },
            {
              front: "A Anthropic oferece um modelo de embeddings próprio?",
              back: "Não de primeira parte; a doc recomenda provedores de embeddings de terceiros. Escolha pela doc oficial em vez de decorar nomes/preços.",
              tags: ["embeddings", "rag"],
            },
            {
              front: "Densa vs. esparsa vs. híbrida na busca?",
              back: "Densa = semântica por embeddings (significado). Esparsa = lexical/BM25 (termos exatos). Híbrida = combina ambas e costuma reranquear; geralmente o melhor resultado.",
              tags: ["rag"],
            },
          ],
          questions: [
            {
              prompt: "Qual é o objetivo principal de RAG?",
              options: [
                "Treinar um novo modelo do zero",
                "Recuperar trechos relevantes e injetá-los no contexto para fundamentar a resposta",
                "Aumentar o max_tokens automaticamente",
                "Eliminar a necessidade de prompts",
              ],
              correctIndex: 1,
              explanation:
                "RAG busca o relevante e o coloca no prompt, fundamentando a resposta em dados atualizados/privados sem re-treinar.",
              difficulty: 1,
              tags: ["rag"],
            },
            {
              prompt: "Por que o chunking importa?",
              options: [
                "Trechos não influenciam a recuperação",
                "Trechos pequenos demais perdem contexto; grandes demais diluem relevância e gastam tokens",
                "Quanto maior o trecho, sempre melhor",
                "Chunking só afeta a latência, não a qualidade",
              ],
              correctIndex: 1,
              explanation:
                "Há um equilíbrio: cortes ruins prejudicam tanto a relevância quanto o custo de tokens.",
              difficulty: 2,
              tags: ["rag"],
            },
            {
              prompt:
                "Para casar termos exatos como códigos de produto e nomes próprios, qual abordagem ajuda mais?",
              options: [
                "Apenas busca densa (semântica)",
                "Busca esparsa/lexical (ex.: BM25), idealmente em híbrido com densa",
                "Aumentar a temperature",
                "Remover o índice",
              ],
              correctIndex: 1,
              explanation:
                "Busca lexical/esparsa é forte em termos exatos; combiná-la com a densa (híbrido) costuma dar o melhor resultado.",
              difficulty: 2,
              tags: ["rag"],
            },
            {
              prompt: "Qual é um erro comum em RAG?",
              options: [
                "Medir a qualidade da recuperação com evals",
                "Recuperar trechos demais e diluir/estourar o contexto",
                "Usar busca híbrida",
                "Reranquear resultados ruidosos",
              ],
              correctIndex: 1,
              explanation:
                "Recuperar contexto em excesso dilui a relevância e gasta tokens. Medir com evals e reranquear são boas práticas, não erros.",
              difficulty: 1,
              tags: ["rag"],
            },
          ],
          labs: [
            {
              title: "Lab: desenhe um pipeline de RAG",
              promptMd: [
                "## Objetivo",
                "",
                "Descreva (em pseudocódigo ou prosa estruturada) um pipeline de RAG para uma base de FAQs de suporte, cobrindo as 6 etapas (chunk, embed, index, retrieve, augment, generate).",
                "",
                "Inclua: estratégia de chunking, escolha de top-k, e como você decidiria entre busca densa, esparsa ou híbrida. Diga como mediria a qualidade da recuperação.",
              ].join("\n"),
              rubric: [
                "Cobre as 6 etapas (chunk, embed, index, retrieve, augment, generate)",
                "Justifica uma estratégia de chunking sensata (ex.: por pergunta/seção)",
                "Define top-k e raciocina sobre o trade-off de muito/pouco contexto",
                "Discute densa vs. esparsa vs. híbrida para o caso",
                "Propõe medir a recuperação (ex.: recall@k num conjunto de teste)",
              ],
              modelAnswer: [
                "1. **Chunk** — uma FAQ por chunk (pergunta + resposta), pois cada par já é uma unidade semântica coesa; evita cortar no meio de uma ideia.",
                "2. **Embed** — gere o embedding de cada chunk com um provedor de terceiros (ver doc oficial).",
                "3. **Index** — armazene os vetores num vector store com metadados (id da FAQ, categoria).",
                "4. **Retrieve** — embed a pergunta do usuário e busque top-k. Comece com k=3-5; aumente só se o recall for baixo.",
                "5. **Augment** — monte o prompt: system com regras de fundamentação + os k trechos + a pergunta. Instrua a responder SOMENTE com base nos trechos.",
                "6. **Generate** — chame a Messages API (tier Sonnet costuma bastar).",
                "",
                "**Busca**: FAQs têm termos exatos (nomes de planos, códigos) — uma abordagem **híbrida** (densa + BM25) com rerank tende a ganhar da densa pura.",
                "",
                "**Medir**: monte um conjunto de teste com perguntas reais rotuladas com o chunk correto e meça **recall@k** (a resposta certa está entre os k recuperados?). Itere o chunking/k até atingir a meta.",
              ].join("\n"),
            },
          ],
        },
        {
          title: "Citations: fundamentar respostas nas fontes",
          readingMd: [
            "# Citations: fundamentar respostas nas fontes",
            "",
            "Recuperar bons trechos é metade do trabalho. A outra metade é fazer o modelo **responder a partir deles** e **citar** de onde tirou cada afirmação — isso reduz alucinação e dá verificabilidade ao usuário.",
            "",
            "## O recurso de Citations da Claude API",
            "",
            "A Claude API tem suporte nativo a **citations**: você fornece documentos (via blocos de conteúdo do tipo `document`) e o modelo retorna, junto do texto, **referências precisas** aos trechos da fonte que embasaram cada parte da resposta. Em vez de pedir citações \"na unha\" pelo prompt (e torcer para o modelo não inventar a referência), o recurso nativo entrega citações **estruturadas** e ancoradas no documento real.",
            "",
            "Esquematicamente:",
            "",
            "```ts",
            "const res = await client.messages.create({",
            "  model: \"<id-do-modelo-da-doc-oficial>\",",
            "  max_tokens: 1024,",
            "  messages: [{",
            "    role: \"user\",",
            "    content: [",
            "      {",
            "        type: \"document\",",
            "        source: { type: \"text\", media_type: \"text/plain\", data: \"<texto da fonte>\" },",
            "        title: \"Política de reembolso\",",
            "        citations: { enabled: true },",
            "      },",
            "      { type: \"text\", text: \"Qual é o prazo de reembolso? Cite a fonte.\" },",
            "    ],",
            "  }],",
            "});",
            "// Os blocos de texto da resposta vêm acompanhados de blocos `citations`",
            "// apontando para os trechos exatos do documento.",
            "```",
            "",
            "## Por que preferir o recurso nativo a \"pedir citação no prompt\"?",
            "",
            "- **Precisão**: as citações nativas referenciam *spans* reais do documento, não texto inventado.",
            "- **Estrutura**: você recebe as referências em campos próprios, fáceis de renderizar (links, destaques).",
            "- **Confiança**: o usuário pode auditar cada afirmação contra a fonte.",
            "",
            "## Boas práticas de fundamentação",
            "",
            "- instrua o modelo (system prompt) a responder **apenas** com base nos documentos fornecidos e a dizer quando **não há** base suficiente (\"não encontrei na fonte\");",
            "- combine citations com um RAG bem ajustado: citação só é útil se os trechos recuperados forem os certos;",
            "- mostre as citações ao usuário final — verificabilidade é parte da confiança.",
            "",
            "## Cuidado",
            "",
            "Pedir \"cite a fonte\" só via texto, sem fornecer a fonte nem o recurso nativo, é um convite à **alucinação de referências** (URLs e páginas que não existem). Sempre ancore em documentos reais.",
            "",
            "> Leitura oficial: https://docs.anthropic.com/en/docs/build-with-claude/citations",
          ].join("\n"),
          flashcards: [
            {
              front: "O que o recurso de Citations da Claude API faz?",
              back: "Permite fornecer documentos (blocos `document`) e receber, junto da resposta, referências estruturadas e precisas aos trechos da fonte que embasaram cada parte do texto.",
              tags: ["citations"],
            },
            {
              front: "Por que citations nativas são melhores que pedir 'cite a fonte' no prompt?",
              back: "Porque referenciam spans reais do documento (não texto inventado), vêm estruturadas e fáceis de renderizar, e permitem auditoria — reduzindo alucinação de referências.",
              tags: ["citations"],
            },
            {
              front: "Como instruir o modelo para fundamentar respostas?",
              back: "No system prompt: responder APENAS com base nos documentos fornecidos e admitir quando não há base suficiente (\"não encontrei na fonte\").",
              tags: ["citations", "rag"],
            },
            {
              front: "Qual o risco de pedir citações só por texto, sem fornecer a fonte?",
              back: "Alucinação de referências: o modelo pode inventar URLs/páginas inexistentes. Sempre ancore em documentos reais (citations nativas).",
              tags: ["citations"],
            },
          ],
          questions: [
            {
              prompt: "Qual a principal vantagem das citations nativas da Claude API?",
              options: [
                "Aumentam o max_tokens",
                "Referenciam trechos reais do documento, de forma estruturada e auditável",
                "Eliminam a necessidade de recuperar documentos",
                "Reduzem o preço por token",
              ],
              correctIndex: 1,
              explanation:
                "As citations nativas ancoram a resposta em spans reais da fonte e as entregam estruturadas, reduzindo alucinação e permitindo auditoria.",
              difficulty: 1,
              tags: ["citations"],
            },
            {
              prompt:
                "Como reduzir alucinação de referências em uma aplicação que precisa citar fontes?",
              options: [
                "Pedir 'cite a fonte' no prompt, sem fornecer documentos",
                "Fornecer os documentos e usar o recurso nativo de citations",
                "Aumentar a temperature",
                "Remover o system prompt",
              ],
              correctIndex: 1,
              explanation:
                "Fornecer a fonte e usar citations nativas ancora as referências no documento real, em vez de deixar o modelo inventar.",
              difficulty: 2,
              tags: ["citations"],
            },
            {
              prompt:
                "O que um bom system prompt para um app fundamentado deve instruir?",
              options: [
                "Responder mesmo sem base, para nunca ficar em silêncio",
                "Responder apenas com base nos documentos e admitir quando não há base",
                "Ignorar os documentos fornecidos",
                "Sempre inventar uma URL plausível",
              ],
              correctIndex: 1,
              explanation:
                "Fundamentação significa responder só com base na fonte e dizer claramente quando a informação não está nela.",
              difficulty: 1,
              tags: ["citations", "rag"],
            },
          ],
        },
      ],
    },

    // =====================================================================
    // MÓDULO 4 — PRODUÇÃO E CONFIABILIDADE
    // =====================================================================
    {
      title: "Produção e confiabilidade",
      lessons: [
        {
          title: "Avaliação: critérios de sucesso e test sets",
          readingMd: [
            "# Avaliação: critérios de sucesso e test sets",
            "",
            "\"Funciona no meu teste manual\" não é confiabilidade. Um desenvolvedor de produção define **critérios de sucesso** e mede com **evals** (conjuntos de avaliação) — esse ciclo é o coração da engenharia de prompts.",
            "",
            "## Bons critérios de sucesso",
            "",
            "São **SMART** no espírito:",
            "",
            "- **Específicos** — \"classificação de sentimento acurada\", não \"boa performance\".",
            "- **Mensuráveis** — métricas quantitativas (F1, acurácia, latência ms) e/ou escalas qualitativas consistentes (Likert 1-5). Mesmo temas \"vagos\" como segurança viram número: \"menos de 0,1% das saídas em 10.000 sinalizadas por toxicidade\".",
            "- **Atingíveis** — ancorados em benchmarks/experimentos, não em fantasia.",
            "- **Relevantes** — alinhados ao propósito (acurácia de citação é crítica num app médico; menos num chatbot casual).",
            "",
            "A maioria dos casos precisa de avaliação **multidimensional** (ex.: F1 ≥ 0,85 **e** 99,5% não-tóxico **e** 95% < 200ms).",
            "",
            "## Princípios de design de evals",
            "",
            "1. **Seja específico da tarefa** — espelhe a distribuição real, incluindo **edge cases** (entrada vazia, longa demais, ambígua, adversarial).",
            "2. **Automatize quando possível** — múltipla escolha, string match, grading por código ou por LLM.",
            "3. **Priorize volume sobre perfeição** — mais casos com grading automático (mesmo com sinal um pouco menor) valem mais que poucos casos hand-graded.",
            "",
            "## Três métodos de grading",
            "",
            "1. **Code-based (por código)** — o mais rápido, confiável e escalável. Ex.: `output == golden_answer` (exact match) ou `key_phrase in output` (string match). Falta nuance para julgamentos complexos.",
            "2. **Human (humano)** — mais flexível e de alta qualidade, porém lento e caro. Evite quando der.",
            "3. **LLM-based (LLM como juiz)** — rápido, flexível e escalável para julgamentos complexos. **Teste a confiabilidade** antes de escalar.",
            "",
            "## Dicas para LLM-as-judge",
            "",
            "- **Rubrica detalhada e clara** (\"deve mencionar 'Acme Inc.' na 1ª frase; senão, incorreto\").",
            "- **Empírico/específico**: peça saída só `correct`/`incorrect` ou nota 1-5 — evite avaliações puramente qualitativas.",
            "- **Encoraje raciocínio**: peça o juiz pensar em `<thinking>` antes de decidir, depois descarte o raciocínio. Melhora a performance em julgamentos difíceis.",
            "- **Use um modelo diferente** para avaliar, idealmente, do que o que gerou a saída.",
            "",
            "## Exemplo de LLM-as-judge (Python)",
            "",
            "```python",
            "import anthropic",
            "client = anthropic.Anthropic()",
            "",
            "def grade(answer, rubric):",
            "    prompt = (",
            "        f\"Avalie a resposta segundo a rubrica:\\n<rubric>{rubric}</rubric>\\n\"",
            "        f\"<answer>{answer}</answer>\\n\"",
            "        \"Raciocine em <thinking> e então produza 'correct' ou 'incorrect' em <result>.\"",
            "    )",
            "    r = client.messages.create(model=\"<id>\", max_tokens=1024,",
            "                               messages=[{\"role\": \"user\", \"content\": prompt}])",
            "    return \"<result>correct</result>\" in r.content[0].text.lower()",
            "```",
            "",
            "> Dica: peça ao próprio Claude para **gerar mais casos de teste** a partir de um conjunto-semente.",
            "",
            "> Leitura oficial: https://docs.anthropic.com/en/docs/test-and-evaluate/develop-tests",
          ].join("\n"),
          flashcards: [
            {
              front: "Quais qualidades um bom critério de sucesso tem?",
              back: "Específico, Mensurável, Atingível e Relevante. Costuma ser multidimensional (ex.: F1 ≥ 0,85 E 99,5% não-tóxico E 95% < 200ms).",
              tags: ["avaliacao"],
            },
            {
              front: "Quais são os três métodos de grading de evals?",
              back: "Code-based (mais rápido/confiável/escalável), Human (flexível mas lento/caro) e LLM-based (rápido e flexível para julgamentos complexos; teste a confiabilidade antes de escalar).",
              tags: ["avaliacao"],
            },
            {
              front: "Princípio: volume vs. qualidade nos evals?",
              back: "Priorize volume: mais casos com grading automático (mesmo com sinal um pouco menor) batem poucos casos hand-graded de alta qualidade.",
              tags: ["avaliacao"],
            },
            {
              front: "Boas práticas de LLM-as-judge?",
              back: "Rubrica clara, saída empírica (correct/incorrect ou 1-5), pedir raciocínio em <thinking> antes de decidir, e usar um modelo diferente do que gerou a saída.",
              tags: ["avaliacao"],
            },
          ],
          questions: [
            {
              prompt:
                "Qual é o método de grading mais rápido, confiável e escalável quando aplicável?",
              options: [
                "Human grading",
                "Code-based grading (exact/string match)",
                "LLM-based grading",
                "Não medir",
              ],
              correctIndex: 1,
              explanation:
                "Code-based é o mais rápido e confiável; falta nuance para julgamentos complexos, onde entram LLM-as-judge ou humanos.",
              difficulty: 1,
              tags: ["avaliacao"],
            },
            {
              prompt: "Qual critério de sucesso é o BOM (mensurável)?",
              options: [
                "O modelo deve classificar bem os sentimentos",
                "F1 ≥ 0,85 num test set de 10.000 tweets diversos, +5% sobre o baseline",
                "As saídas devem ser seguras",
                "A resposta deve ser boa",
              ],
              correctIndex: 1,
              explanation:
                "Critérios bons são específicos, mensuráveis, atingíveis e relevantes. \"Classificar bem\" e \"ser seguro\" são vagos.",
              difficulty: 2,
              tags: ["avaliacao"],
            },
            {
              prompt: "Por que pedir ao juiz LLM para raciocinar antes de pontuar?",
              options: [
                "Para gastar mais tokens de propósito",
                "Porque melhora a performance em julgamentos complexos (depois descarta-se o raciocínio)",
                "Porque a API exige um bloco thinking",
                "Para reduzir o max_tokens",
              ],
              correctIndex: 1,
              explanation:
                "Pedir raciocínio em <thinking> antes da decisão melhora a qualidade do julgamento; o raciocínio é descartado e fica só o veredito.",
              difficulty: 2,
              tags: ["avaliacao"],
            },
            {
              prompt: "Por que incluir edge cases nos evals?",
              options: [
                "Para inflar artificialmente a acurácia",
                "Para espelhar a distribuição real, onde entradas raras/adversariais acontecem",
                "Porque a doc proíbe casos normais",
                "Edge cases não importam para evals",
              ],
              correctIndex: 1,
              explanation:
                "Evals devem espelhar a distribuição real, incluindo entradas vazias, longas, ambíguas ou adversariais — é onde sistemas costumam falhar.",
              difficulty: 1,
              tags: ["avaliacao"],
            },
          ],
          labs: [
            {
              title: "Lab: escreva um eval automatizado",
              promptMd: [
                "## Objetivo",
                "",
                "Para um classificador de sentimento (positive/negative/neutral/mixed), escreva (TS ou Python):",
                "",
                "1. um pequeno **test set** com 4-5 casos rotulados, incluindo ao menos 1 **edge case** (ex.: sarcasmo ou sentimento misto);",
                "2. uma função de **grading por código** (exact match normalizado);",
                "3. o cálculo da **acurácia**.",
                "",
                "Depois, explique em 2-3 frases quando você trocaria o grading por código por um **LLM-as-judge**.",
              ].join("\n"),
              rubric: [
                "Test set com casos rotulados e pelo menos 1 edge case explícito",
                "Função de exact match que normaliza caixa/espaços",
                "Cálculo correto da acurácia sobre o test set",
                "Chamada à Messages API para obter a classificação",
                "Justifica quando migrar para LLM-as-judge (julgamentos com nuance)",
              ],
              modelAnswer: [
                "```python",
                "import anthropic",
                "client = anthropic.Anthropic()",
                "",
                "tweets = [",
                "    {\"text\": \"Que filme maravilhoso!\", \"label\": \"positive\"},",
                "    {\"text\": \"Perda total de tempo.\", \"label\": \"negative\"},",
                "    {\"text\": \"O pedido chegou no prazo.\", \"label\": \"neutral\"},",
                "    {\"text\": \"Adoro quando meu voo atrasa 5h. #melhordia\", \"label\": \"negative\"},  # edge: sarcasmo",
                "    {\"text\": \"Enredo fraco, mas atuação ótima.\", \"label\": \"mixed\"},  # edge: misto",
                "]",
                "",
                "def classificar(texto):",
                "    r = client.messages.create(model=\"<id>\", max_tokens=10,",
                "        messages=[{\"role\": \"user\", \"content\":",
                "            f\"Classifique como 'positive','negative','neutral' ou 'mixed': {texto}\"}])",
                "    return r.content[0].text",
                "",
                "def exact_match(out, gold):",
                "    return out.strip().lower() == gold.lower()",
                "",
                "acertos = sum(exact_match(classificar(t['text']), t['label']) for t in tweets)",
                "print(f\"Acurácia: {acertos / len(tweets) * 100:.1f}%\")",
                "```",
                "",
                "Quando migrar para LLM-as-judge: quando o veredito exige nuance que regras não capturam (ex.: avaliar tom, coerência ou fidelidade factual de uma resposta aberta). Aí defina uma rubrica clara, peça raciocínio antes da nota e valide a confiabilidade do juiz antes de escalar.",
              ].join("\n"),
            },
          ],
        },
        {
          title: "Erros, retries e idempotência",
          readingMd: [
            "# Erros, retries e idempotência",
            "",
            "Produção significa lidar com falhas com elegância. A Claude API usa códigos HTTP previsíveis.",
            "",
            "## Tabela de erros (essencial decorar)",
            "",
            "| HTTP | type | Significado | Retry? |",
            "| --- | --- | --- | --- |",
            "| 400 | `invalid_request_error` | Formato/conteúdo inválido | Não (corrija a requisição) |",
            "| 401 | `authentication_error` | Problema com a API key | Não (verifique a chave) |",
            "| 403 | `permission_error` | Sem permissão para o recurso | Não |",
            "| 404 | `not_found_error` | Recurso não encontrado | Não |",
            "| 413 | `request_too_large` | Excede o limite de bytes | Não (reduza o payload) |",
            "| 429 | `rate_limit_error` | Bateu no rate limit | **Sim**, com backoff |",
            "| 500 | `api_error` | Erro interno inesperado | **Sim**, com backoff |",
            "| 529 | `overloaded_error` | API temporariamente sobrecarregada | **Sim**, com backoff |",
            "",
            "Regra mental: **4xx (exceto 429)** = erro **seu**, corrija e não repita igual. **429/5xx/529** = transitório, **faça retry com backoff exponencial + jitter**.",
            "",
            "## Formato do erro",
            "",
            "Erros vêm como JSON com um objeto `error` que sempre tem `type` e `message`, mais um `request_id`:",
            "",
            "```json",
            "{",
            "  \"type\": \"error\",",
            "  \"error\": { \"type\": \"not_found_error\", \"message\": \"The requested resource could not be found.\" },",
            "  \"request_id\": \"req_011CSHoEeqs5C35K2UUqR7Fy\"",
            "}",
            "```",
            "",
            "## SDKs lançam exceções tipadas",
            "",
            "Não faça string-matching da mensagem. Capture as **classes tipadas** do SDK, da mais específica para a mais genérica. Ex. (Python): `anthropic.NotFoundError`, `anthropic.RateLimitError`, etc.",
            "",
            "```python",
            "import anthropic, time, random",
            "client = anthropic.Anthropic()",
            "",
            "def chamar_com_retry(**kwargs):",
            "    for tentativa in range(5):",
            "        try:",
            "            return client.messages.create(**kwargs)",
            "        except (anthropic.RateLimitError, anthropic.InternalServerError) as e:",
            "            espera = (2 ** tentativa) + random.random()  # backoff + jitter",
            "            time.sleep(espera)",
            "        except anthropic.APIStatusError as e:",
            "            if e.status_code == 529:  # overloaded",
            "                time.sleep((2 ** tentativa) + random.random())",
            "            else:",
            "                raise  # 4xx: não adianta repetir igual",
            "    raise RuntimeError(\"falhou após retries\")",
            "```",
            "",
            "## Backoff exponencial + jitter",
            "",
            "Espere 1s, 2s, 4s, 8s... **com jitter** (um aleatório) para evitar o \"thundering herd\" (todos os clientes batendo ao mesmo tempo). Defina um **teto de tentativas** e propague o erro depois disso. Os SDKs oficiais já fazem retries automáticos de erros transitórios por padrão.",
            "",
            "## Streaming e long requests",
            "",
            "Em SSE, um erro pode ocorrer **após** o 200 inicial — trate erros no meio do stream. Para gerações longas (>10 min), prefira streaming ou o Batches API, e cuide de timeouts de conexão ociosa.",
            "",
            "## request-id para suporte",
            "",
            "Guarde o `request-id` (header / `message._request_id`) nos seus logs: é o identificador que o suporte da Anthropic usa.",
            "",
            "> Leitura oficial: https://docs.anthropic.com/en/api/errors",
          ].join("\n"),
          flashcards: [
            {
              front: "Quais códigos justificam retry com backoff?",
              back: "429 (rate_limit_error), 500 (api_error) e 529 (overloaded_error) — erros transitórios. 4xx (exceto 429) são erros seus: corrija, não repita igual.",
              tags: ["erros"],
            },
            {
              front: "O que sempre há no objeto `error` de uma resposta de erro?",
              back: "`type` e `message`. A resposta também traz `request_id`.",
              tags: ["erros"],
            },
            {
              front: "Por que usar jitter no backoff?",
              back: "Para evitar 'thundering herd' — todos os clientes repetindo no mesmo instante. O jitter espalha as tentativas no tempo.",
              tags: ["erros"],
            },
            {
              front: "Como tratar erros com os SDKs oficiais?",
              back: "Capturando as classes de exceção tipadas (ex.: anthropic.RateLimitError, anthropic.NotFoundError), da mais específica para a mais genérica — nunca por string-matching da mensagem.",
              tags: ["erros", "sdk"],
            },
          ],
          questions: [
            {
              prompt: "Você recebe um 429 (rate_limit_error). Qual a ação correta?",
              options: [
                "Corrigir o JSON da requisição",
                "Retry com backoff exponencial e jitter",
                "Trocar a API key",
                "Reduzir o payload abaixo do limite de bytes",
              ],
              correctIndex: 1,
              explanation:
                "429 é transitório (rate limit). Faça retry com backoff exponencial + jitter, respeitando um teto de tentativas.",
              difficulty: 1,
              tags: ["erros"],
            },
            {
              prompt: "Um 400 (invalid_request_error) deve ser tratado como?",
              options: [
                "Transitório: retry imediato em loop",
                "Erro seu: corrija a requisição; repetir igual não resolve",
                "Sempre ignorar",
                "Sinal para trocar de modelo",
              ],
              correctIndex: 1,
              explanation:
                "4xx (exceto 429) indicam problema na sua requisição. Repetir a mesma chamada não ajuda — corrija o conteúdo/forma.",
              difficulty: 1,
              tags: ["erros"],
            },
            {
              prompt: "Qual a melhor forma de detectar o tipo de erro nos SDKs?",
              options: [
                "Comparar substrings da mensagem de erro",
                "Capturar as classes de exceção tipadas (mais específica primeiro)",
                "Ignorar exceções",
                "Ler o corpo HTML da resposta",
              ],
              correctIndex: 1,
              explanation:
                "Os SDKs lançam exceções tipadas; capture-as por classe (da mais específica à mais genérica), não por string-matching.",
              difficulty: 2,
              tags: ["erros", "sdk"],
            },
            {
              prompt: "Para que serve guardar o `request_id` nos logs?",
              options: [
                "Para calcular o custo",
                "Para identificar a requisição ao abrir um chamado com o suporte da Anthropic",
                "Para autenticar a próxima chamada",
                "Para aumentar o rate limit",
              ],
              correctIndex: 1,
              explanation:
                "O request_id identifica unicamente a requisição e é o que o suporte usa para investigar.",
              difficulty: 1,
              tags: ["erros", "observabilidade"],
            },
          ],
          labs: [
            {
              title: "Lab: wrapper de chamada com retry resiliente",
              promptMd: [
                "## Objetivo",
                "",
                "Escreva (TS ou Python) um wrapper de `messages.create` que:",
                "",
                "1. faz **retry apenas** em erros transitórios (429, 500, 529);",
                "2. usa **backoff exponencial com jitter**;",
                "3. respeita um **teto de tentativas** e propaga o erro depois;",
                "4. **não** repete em 4xx (exceto 429) e registra o `request_id` quando disponível.",
              ].join("\n"),
              rubric: [
                "Distingue erros transitórios (429/500/529) de erros do cliente (4xx)",
                "Implementa backoff exponencial COM jitter",
                "Tem teto de tentativas e propaga o erro ao esgotá-las",
                "Captura exceções tipadas do SDK (não string-matching)",
                "Registra/loga o request_id para depuração",
              ],
              modelAnswer: [
                "```python",
                "import anthropic, time, random, logging",
                "client = anthropic.Anthropic()",
                "TRANSIENT = (anthropic.RateLimitError, anthropic.InternalServerError)",
                "",
                "def criar(max_tentativas=5, **kwargs):",
                "    for i in range(max_tentativas):",
                "        try:",
                "            return client.messages.create(**kwargs)",
                "        except TRANSIENT:",
                "            time.sleep((2 ** i) + random.random())  # backoff + jitter",
                "        except anthropic.APIStatusError as e:",
                "            rid = getattr(e, \"request_id\", None)",
                "            if e.status_code == 529:  # overloaded → transitório",
                "                logging.warning(\"529 overloaded (request_id=%s)\", rid)",
                "                time.sleep((2 ** i) + random.random())",
                "            else:  # 4xx do cliente → não repete",
                "                logging.error(\"erro do cliente %s (request_id=%s)\", e.status_code, rid)",
                "                raise",
                "    raise RuntimeError(\"esgotou as tentativas\")",
                "```",
                "",
                "Notas: 429/500/529 são transitórios (retry); 400/401/403/404/413 são erros seus (corrija, não repita). O jitter evita thundering herd. Os SDKs oficiais já fazem retries automáticos — este wrapper ilustra a lógica e adiciona logging do request_id.",
              ].join("\n"),
            },
          ],
        },
        {
          title: "Prompt caching, custo e observabilidade",
          readingMd: [
            "# Prompt caching, custo e observabilidade",
            "",
            "## Prompt caching: corte custo e latência",
            "",
            "Se você reenvia o **mesmo prefixo** (system prompt longo, instruções, exemplos, documentos) em muitas requisições, o **prompt caching** evita reprocessá-lo: marca-se um ponto de corte e o prefixo fica em cache.",
            "",
            "Adicione `cache_control` ao último bloco estável do prefixo:",
            "",
            "```ts",
            "const res = await client.messages.create({",
            "  model: \"<id-do-modelo-da-doc-oficial>\",",
            "  max_tokens: 1024,",
            "  system: [",
            "    { type: \"text\", text: \"<instruções longas e estáveis>\",",
            "      cache_control: { type: \"ephemeral\" } },",
            "  ],",
            "  messages: [{ role: \"user\", content: \"...\" }],",
            "});",
            "```",
            "",
            "### Como funciona",
            "",
            "- Hierarquia do que pode ser cacheado: **`tools` → `system` → `messages`** (o prefixo é cacheado nessa ordem).",
            "- O sistema verifica se o prefixo já está em cache; se sim, lê do cache (mais barato e rápido); se não, processa e grava.",
            "- **Mínimo de tokens** para cachear varia por modelo (tiers maiores exigem mais; verifique a doc). Prefixos curtos demais não são cacheados (e não dá erro).",
            "- **TTL**: padrão **5 minutos** (`ephemeral`); há opção de **1 hora** (`ttl: \"1h\"`), a custo de escrita maior.",
            "- Marque com `cache_control` o **último bloco que permanece idêntico** entre requisições — nunca blocos que mudam (timestamps, id do usuário).",
            "",
            "### Medindo o cache (no `usage`)",
            "",
            "- **`cache_creation_input_tokens`** — tokens gravados no cache (primeira vez).",
            "- **`cache_read_input_tokens`** — tokens lidos do cache (muito mais baratos).",
            "- **`input_tokens`** — tokens após o último breakpoint (não cacheados).",
            "",
            "Confirme o cache hit checando `cache_read_input_tokens > 0`. **Economia**: leituras de cache custam uma fração do preço de entrada; escritas custam um pouco mais que a entrada normal. Para os multiplicadores e o preço exato, consulte a doc oficial.",
            "",
            "## Custo: pense por token e por tier",
            "",
            "- O custo vem de **input tokens + output tokens** (e, em server tools, cobranças adicionais por uso).",
            "- Tool use adiciona tokens (definições de `tools`, blocos `tool_use`/`tool_result`).",
            "- Alavancas: escolher o **tier** certo (Haiku para volume simples, Sonnet padrão, Opus só quando preciso), **cachear** prefixos, encurtar prompts, e usar o **Batches API** para cargas assíncronas. Não decore preços — leia a tabela oficial.",
            "",
            "## Observabilidade",
            "",
            "Logue, para cada chamada: **modelo**, **usage** (input/output/cache), **stop_reason**, **latência**, **request_id** e erros. Isso permite: rastrear custo, detectar regressões, depurar com o suporte (request_id) e monitorar saúde. No Agent SDK, use **hooks** (PostToolUse etc.) como pontos naturais de telemetria e auditoria.",
            "",
            "> Leitura oficial: https://docs.anthropic.com/en/docs/build-with-claude/prompt-caching",
          ].join("\n"),
          flashcards: [
            {
              front: "Como ativar prompt caching e onde marcar o breakpoint?",
              back: "Adicionando `cache_control: { type: \"ephemeral\" }` ao ÚLTIMO bloco estável do prefixo. Hierarquia cacheável: tools → system → messages.",
              tags: ["prompt-caching"],
            },
            {
              front: "Qual o TTL padrão do cache e que opção estendida existe?",
              back: "Padrão 5 minutos (ephemeral); opção de 1 hora via `ttl: \"1h\"`, a um custo de escrita maior.",
              tags: ["prompt-caching"],
            },
            {
              front: "Quais campos de `usage` medem o cache?",
              back: "cache_creation_input_tokens (gravados), cache_read_input_tokens (lidos, mais baratos) e input_tokens (após o breakpoint, não cacheados). Cache hit: cache_read_input_tokens > 0.",
              tags: ["prompt-caching", "observabilidade"],
            },
            {
              front: "O que NÃO marcar com cache_control?",
              back: "Blocos que mudam entre requisições (timestamps, id de usuário). Marque o último bloco que permanece idêntico — caso contrário o cache não acerta.",
              tags: ["prompt-caching"],
            },
          ],
          questions: [
            {
              prompt: "Onde se deve colocar o conteúdo cacheável e o cache_control?",
              options: [
                "No fim do prompt, em blocos que mudam a cada chamada",
                "No início (prefixo estável), marcando o último bloco idêntico com cache_control",
                "Apenas no array messages, nunca em system/tools",
                "Em qualquer lugar; a ordem não importa",
              ],
              correctIndex: 1,
              explanation:
                "Cacheia-se o prefixo estável (tools → system → messages). Marque o último bloco que permanece idêntico; blocos voláteis quebram o cache.",
              difficulty: 2,
              tags: ["prompt-caching"],
            },
            {
              prompt: "Como confirmar que houve cache hit?",
              options: [
                "Checando stop_reason",
                "Verificando cache_read_input_tokens > 0 no usage",
                "Olhando o request_id",
                "Contando os content blocks",
              ],
              correctIndex: 1,
              explanation:
                "cache_read_input_tokens > 0 indica que o prefixo foi lido do cache (leitura barata).",
              difficulty: 2,
              tags: ["prompt-caching", "observabilidade"],
            },
            {
              prompt: "Qual é uma alavanca legítima de redução de custo?",
              options: [
                "Usar sempre o tier Opus para tudo",
                "Escolher o tier adequado, cachear prefixos e encurtar prompts",
                "Aumentar a temperature",
                "Remover o usage dos logs",
              ],
              correctIndex: 1,
              explanation:
                "Custo = input + output tokens. Ajustar o tier, cachear prefixos estáveis e enxugar prompts reduz o gasto. Opus para tudo é desperdício.",
              difficulty: 1,
              tags: ["prompt-caching"],
            },
            {
              prompt: "O que é essencial logar para observabilidade de uma app com Claude?",
              options: [
                "Apenas o texto da resposta",
                "Modelo, usage, stop_reason, latência e request_id",
                "Somente a API key",
                "Nada; logs atrapalham",
              ],
              correctIndex: 1,
              explanation:
                "Logar modelo, usage (incl. cache), stop_reason, latência e request_id permite rastrear custo, detectar regressões e depurar com o suporte.",
              difficulty: 1,
              tags: ["observabilidade"],
            },
          ],
          labs: [
            {
              title: "Lab: aplique prompt caching e meça a economia",
              promptMd: [
                "## Objetivo",
                "",
                "Você tem um assistente com um **system prompt longo e estável** (políticas + exemplos) e muitas requisições por minuto. Escreva (TS ou Python):",
                "",
                "1. a chamada com `cache_control` no bloco estável do `system`;",
                "2. a leitura de `cache_creation_input_tokens` e `cache_read_input_tokens` do `usage`;",
                "3. uma verificação que loga se houve **cache hit**.",
                "",
                "Explique por que NÃO colocar dados voláteis (timestamp) dentro do bloco cacheado e quando usar TTL de 1h.",
              ].join("\n"),
              rubric: [
                "Coloca cache_control { type: \"ephemeral\" } no bloco estável do system",
                "Mantém dados voláteis FORA do bloco cacheado",
                "Lê cache_creation_input_tokens e cache_read_input_tokens do usage",
                "Detecta/loga o cache hit (cache_read_input_tokens > 0)",
                "Explica o TTL de 1h e o trade-off de custo de escrita",
              ],
              modelAnswer: [
                "```python",
                "import anthropic, logging",
                "client = anthropic.Anthropic()",
                "",
                "SYSTEM_ESTAVEL = \"<políticas longas + exemplos, idênticas entre chamadas>\"",
                "",
                "def responder(pergunta: str):",
                "    res = client.messages.create(",
                "        model=\"<id-da-doc>\",",
                "        max_tokens=1024,",
                "        system=[{",
                "            \"type\": \"text\",",
                "            \"text\": SYSTEM_ESTAVEL,",
                "            \"cache_control\": {\"type\": \"ephemeral\"},  # use ttl: \"1h\" se reusar por mais de 5 min",
                "        }],",
                "        messages=[{\"role\": \"user\", \"content\": pergunta}],",
                "    )",
                "    u = res.usage",
                "    if getattr(u, \"cache_read_input_tokens\", 0) > 0:",
                "        logging.info(\"cache HIT: %s tokens lidos do cache\", u.cache_read_input_tokens)",
                "    else:",
                "        logging.info(\"cache MISS/escrita: %s\", getattr(u, \"cache_creation_input_tokens\", 0))",
                "    return res",
                "```",
                "",
                "Dados voláteis (timestamp, id do usuário) NÃO entram no bloco cacheado: qualquer mudança no prefixo invalida o cache e força reescrita, anulando a economia. Coloque o que varia em blocos posteriores (no array messages).",
                "",
                "TTL de 1h: vale quando o mesmo prefixo é reutilizado por mais de 5 minutos (sessões longas, lotes). A escrita de 1h custa mais que a de 5 min, mas se você lê muito do cache ao longo da hora, compensa. Para os multiplicadores exatos, veja a doc oficial.",
              ].join("\n"),
            },
          ],
        },
      ],
    },
  ],
};
